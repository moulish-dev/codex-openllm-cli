{
  "name": "codex-openllm",
  "description": "Codex CLI fork with pluggable LLM backends like Ollama or OpenAI",
  "scripts": {
    "release": "cd codex-cli && npm run release",
    "format": "prettier --check *.json *.md .github/workflows/*.yml",
    "format:fix": "prettier --write *.json *.md .github/workflows/*.yml"
  },
  "devDependencies": {
    "prettier": "^3.5.3"
  },
  "dependencies": {
    "typescript": "^5.8.3"
  }
}
